from django.urls import path
from . import views # импортируем из ЭТОЙ ЖЕ ПАПКИ ПРИЛОЖЕНИЯ файл views

# В файле urls.py КОНКРЕТНОГО приложения, маршрутизация считается, начиная с адреса ПРИЛОЖЕНИЯ. Например, этот файл находится в папке приложения audith, и подключен в глобальный urls.py всего проекта -> все адреса, описанные в этом файла, УЖЕ ПО УМОЛЧАНИЮ начинаются с адреса 127.0.0.1:8000/audith

urlpatterns = [
    path('', views.index, name="audith"),
]